{
  "build": {
    "dockerfileBase": "ghcr.io/railwayapp/railpack-runtime:mise-2026.7.6",
    "dockerfileBuilder": "ghcr.io/railwayapp/railpack-builder:mise-2026.7.6",
    "packages": {
      "apt": [
        "libatomic1"
      ],
      "mise": [
        "node@22"
      ]
    },
    "steps": {
      "install": "npm ci",
      "build": "npm install && npx prisma generate --schema=backend/prisma/schema.prisma && npx prisma migrate resolve --schema=backend/prisma/schema.prisma --applied 0_init || true && npx prisma migrate deploy --schema=backend/prisma/schema.prisma && cd backend && npm run build"
    }
  },
  "deploy": {
    "startCommand": "cd backend && npm run start:prod",
    "restartPolicy": {
      "type": "on_failure",
      "maxRetries": 5
    }
  }
}
